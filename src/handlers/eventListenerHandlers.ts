// export const eventListenersHandlers = {
//     receiveMessageHandler: undefined,
// };

// export function setReceiveMessageHandler(callback) {
//     const shouldRegister = eventListenersHandlers.receiveMessageHandler === undefined;

//     eventListenersHandlers.receiveMessageHandler = callback;
//     if (shouldRegister) {
//         window.addEventListener("message", event => eventListenersHandlers.receiveMessageHandler(event));
//     }
// };
